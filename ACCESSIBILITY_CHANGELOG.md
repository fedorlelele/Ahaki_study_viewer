# Accessibility Changelog

対象: `web_app/index.html`, `web_app/simple.html`, `web_app/tag_study.html`, `web_app/print_export.html`, `web_app/confirm.html`

| 達成基準 | 対応内容 |
|---|---|
| SC 1.3.1 情報及び関係性 | `main` 到達先、見出し階層、フォームラベル、動的フォーム名の補完を整理。 |
| SC 1.3.5 入力目的の特定 | メール・パスワード入力に `autocomplete` を設定。 |
| SC 1.4.1 色の使用 | 正誤状態に「〇 正解」「✕ 不正解」のテキストを併記。 |
| SC 1.4.3 コントラスト（最低限） | 補助文字サイズと主要テキスト色を確認し、タグ学習ラボの薄い罫線色を調整。 |
| SC 1.4.4 テキストのサイズ変更 | 8px/11px の極小表示を12px以上へ調整。 |
| SC 1.4.10 リフロー | タップ領域と折り返し前提の既存レイアウトを維持し、主要操作の最小高さを補強。 |
| SC 1.4.11 非テキストのコントラスト | フォーカスリングとUI境界色を3:1以上を目標に調整。 |
| SC 1.4.12 テキストの間隔 | 固定高さを増やす変更は避け、選択肢ボタンを余白で拡張。 |
| SC 2.1.2 キーボードトラップなし | `simple.html` の確認・結果ダイアログにフォーカストラップと復帰処理を追加。 |
| SC 2.1.4 文字キーのショートカット | `index.html` にショートカット有効/無効トグルと一覧を追加。 |
| SC 2.4.1 ブロックスキップ | 学習者向け主要ページに本文スキップリンクを追加。 |
| SC 2.4.3 フォーカス順序 | ダイアログ表示時の初期フォーカス、閉鎖時の復帰、開閉ボタン状態を補完。 |
| SC 2.4.6 見出し及びラベル | `h4` の飛びを解消し、入力目的が分かるラベルを追加。 |
| SC 2.4.7 フォーカスの可視化 | 全対象ページに `:focus-visible` のグローバル表示を追加。 |
| SC 3.3.1 エラーの特定 | 認証・入力エラー表示をフォーム近傍のテキストとライブ領域で通知。 |
| SC 3.3.2 ラベル又は説明 | placeholder だけに依存しないラベルまたは `aria-label` を補完。 |
| SC 3.3.3 エラー修正の提案 | 既存の修正案内文を維持し、通知領域で読み上げ可能にした。 |
| SC 4.1.2 名前・役割・値 | アイコンボタン、開閉ボタン、検索履歴、動的入力のアクセシブル名と状態を補完。 |
| SC 4.1.3 ステータスメッセージ | 検索件数、正誤結果、タグ選択、プリント生成をライブリージョンで通知。 |

検証メモ:

- インライン JavaScript 構文チェック: 通過。
- ローカル構造チェック（スキップリンク、ライブリージョン、ラベル、autocomplete、冗長 role）: 通過。
- 主要カラー組み合わせのコントラストスポットチェック: 通過。
- `axe-core` CLI は npm registry へのネットワークアクセスが必要だったため、この環境では実行不可。
